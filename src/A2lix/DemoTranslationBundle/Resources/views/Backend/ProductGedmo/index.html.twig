{% extends "A2lixDemoTranslationBundle:Backend:layout.html.twig" %}

{% block content %}
    <h2>ProductGedmo list</h2>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('backend_productGedmo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity }}</td>
                <td>{{ entity.description }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('backend_productGedmo_show', { 'id': entity.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('backend_productGedmo_edit', { 'id': entity.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('backend_productGedmo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
{% endblock %}