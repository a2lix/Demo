{% extends "demo_translation/layout.html.twig" %}

{% block content %}
    <h2>Company list</h2>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Title</th>
                <th>Description</th>
                <th>Categories</th>
                <th>Media</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('a2lix_backend_company_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.code }}</td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.description }}</td>
                <td>{% for categ in entity.categories %}{{ categ.title }}{% endfor %}</td>
                <td>{{ entity.medias|first ? (entity.medias|first).url }}</td>
                <td>
                    <a class="btn btn-default" role="button" href="{{ path('a2lix_backend_company_show', { 'id': entity.id }) }}">show</a>
                    <a class="btn btn-default" role="button" href="{{ path('a2lix_backend_company_edit', { 'id': entity.id }) }}">edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-default" role="button" href="{{ path('a2lix_backend_company_new') }}">
        Create a new entry
    </a>
{% endblock %}