{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label|lower }}" role="alert">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
